<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BlurBerry.Views.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:BlurBerry.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:BlurBerry.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:HomePageViewModel,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Page Header  -->
        <Grid Grid.Row="0" Margin="36,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                VerticalAlignment="Center"
                FontSize="42"
                Text="홈" />

            <SplitButton
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Command="{Binding OpenFileCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="13" Glyph="&#xE838;" />
                    <TextBlock Text="파일 열기" />
                </StackPanel>

                <SplitButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Margin="5" Command="{Binding OpenFileCommand}">
                            <MenuFlyoutItem.Template>
                                <ControlTemplate>
                                    <Grid
                                        x:Name="RootGrid"
                                        Padding="10,5"
                                        CornerRadius="3">
                                        <Grid.Background>
                                            <SolidColorBrush Color="Transparent" />
                                        </Grid.Background>

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <FontIcon
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            FontSize="16"
                                            Glyph="&#xE838;" />

                                        <StackPanel
                                            Grid.Column="1"
                                            Margin="13,0,0,0"
                                            VerticalAlignment="Center">
                                            <TextBlock Text="파일 열기" />
                                            <TextBlock
                                                FontSize="12"
                                                Foreground="DarkGray"
                                                Text="재생할 파일 찾아보기" />
                                        </StackPanel>

                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <VisualState x:Name="PointerOver">
                                                    <VisualState.Setters>
                                                        <Setter Target="RootGrid.Background" Value="{ThemeResource MenuFlyoutItemBackgroundPointerOver}" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                                <VisualState x:Name="Pressed">
                                                    <VisualState.Setters>
                                                        <Setter Target="RootGrid.Background" Value="{ThemeResource MenuFlyoutItemBackgroundPressed}" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                    </Grid>
                                </ControlTemplate>
                            </MenuFlyoutItem.Template>
                        </MenuFlyoutItem>

                        <MenuFlyoutItem Margin="5" Command="{Binding OpenFolderCommand}">
                            <MenuFlyoutItem.Template>
                                <ControlTemplate>
                                    <Grid
                                        x:Name="RootGrid"
                                        Padding="10,5"
                                        CornerRadius="3">
                                        <Grid.Background>
                                            <SolidColorBrush Color="Transparent" />
                                        </Grid.Background>

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <FontIcon
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            FontSize="16"
                                            Glyph="&#xE8B7;" />

                                        <StackPanel
                                            Grid.Column="1"
                                            Margin="13,0,0,0"
                                            VerticalAlignment="Center">
                                            <TextBlock Text="폴더 열기" />
                                            <TextBlock
                                                FontSize="12"
                                                Foreground="DarkGray"
                                                Text="폴더를 선택하고 해당 폴더의 모든 미디어를 가져옵니다." />
                                        </StackPanel>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal" />
                                                <VisualState x:Name="PointerOver">
                                                    <VisualState.Setters>
                                                        <Setter Target="RootGrid.Background" Value="{ThemeResource MenuFlyoutItemBackgroundPointerOver}" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                                <VisualState x:Name="Pressed">
                                                    <VisualState.Setters>
                                                        <Setter Target="RootGrid.Background" Value="{ThemeResource MenuFlyoutItemBackgroundPressed}" />
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>
                                    </Grid>
                                </ControlTemplate>
                            </MenuFlyoutItem.Template>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </SplitButton.Flyout>
            </SplitButton>
        </Grid>

        <!--  Content Area  -->
        <Grid Grid.Row="1" Margin="36,0,36,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                FontSize="20"
                Text="최근 미디어" />

            <GridView
                Grid.Row="1"
                Margin="0,12,0,0"
                SelectionMode="None">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>

                <GridViewItem Margin="0,0,10,10">
                    <Grid Width="160" Height="120">
                        <Rectangle Fill="LightGray" />
                        <StackPanel
                            Padding="8"
                            VerticalAlignment="Bottom"
                            Background="#80000000">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="White"
                                Text="미디어 1" />
                            <TextBlock Foreground="White" Text="00:00" />
                        </StackPanel>
                    </Grid>
                </GridViewItem>
                <GridViewItem Margin="0,0,10,10">
                    <Grid Width="160" Height="120">
                        <Rectangle Fill="DarkGray" />
                        <StackPanel
                            Padding="8"
                            VerticalAlignment="Bottom"
                            Background="#80000000">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="White"
                                Text="미디어 2" />
                            <TextBlock Foreground="White" Text="00:00" />
                        </StackPanel>
                    </Grid>
                </GridViewItem>
                <GridViewItem Margin="0,0,10,10">
                    <Grid Width="160" Height="120">
                        <Rectangle Fill="Gray" />
                        <StackPanel
                            Padding="8"
                            VerticalAlignment="Bottom"
                            Background="#80000000">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="White"
                                Text="미디어 3" />
                            <TextBlock Foreground="White" Text="00:00" />
                        </StackPanel>
                    </Grid>
                </GridViewItem>
                <GridViewItem Margin="0,0,10,10">
                    <Grid Width="160" Height="120">
                        <Rectangle Fill="LightSlateGray" />
                        <StackPanel
                            Padding="8"
                            VerticalAlignment="Bottom"
                            Background="#80000000">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="White"
                                Text="미디어 4" />
                            <TextBlock Foreground="White" Text="00:00" />
                        </StackPanel>
                    </Grid>
                </GridViewItem>
                <GridViewItem Margin="0,0,10,10">
                    <Grid Width="160" Height="120">
                        <Rectangle Fill="SlateGray" />
                        <StackPanel
                            Padding="8"
                            VerticalAlignment="Bottom"
                            Background="#80000000">
                            <TextBlock
                                FontWeight="Bold"
                                Foreground="White"
                                Text="미디어 5" />
                            <TextBlock Foreground="White" Text="00:00" />
                        </StackPanel>
                    </Grid>
                </GridViewItem>
            </GridView>
        </Grid>
    </Grid>
</Page>
